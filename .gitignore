# Dependencies
node_modules/

# Secrets — ห้าม commit ขึ้น GitHub เด็ดขาด
Secret Key.env
credentials/*.json
credentials/*.txt

# Temp / scratch files
test_*_tmp.js
*.tmp.js

# Logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# Local agent settings (เฉพาะเครื่อง)
.claude/

# ข้อมูลรายงาน/ลูกค้า — ห้าม push ขึ้น GitHub (เช่น รายงานร้องเรียนศิริราช)
*.xlsx
